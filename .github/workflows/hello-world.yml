name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on:
  push:
    branches:
      - main
jobs:
  my-simple-job:
    runs-on: ubuntu-latest
    steps:
        - name: tell hello-world
          uses: actions/checkout@v3
        - name: echo hello-world
          run: echo "hello-world"
